[build]
builder = "NIXPACKS"
buildCommand = "chmod +x ./mvnw && ./mvnw clean package -DskipTests"

[deploy]
startCommand = "java -jar target/mahasetu-interop-backend-1.0.0-phase2.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
